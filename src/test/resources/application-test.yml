spring:
  test:
    database:
      replace: none
  datasource:
    url:      jdbc:h2:mem:spring_spock_example;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: root
    password: root
    hikari:
      driver-class-name: org.h2.Driver
